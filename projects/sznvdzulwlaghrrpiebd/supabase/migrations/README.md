# Pending baseline

No migration is authored here yet. The hosted project was discovered and read back, but the repository baseline remains `pending` until a reviewed `supabase db pull` (or equivalent approved baseline workflow) produces provenance-bound migration evidence.

Do not synthesize production DDL from catalog metadata or rewrite an observed migration history.
