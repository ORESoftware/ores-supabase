# Edge Functions

No Edge Function is admitted for this target yet. Add functions only from a reviewed product requirement with authentication defined explicitly; do not disable JWT verification by default and never store project secrets in this tree.
